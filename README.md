# ansible.Bootstrap
pull system to usable from fresh install

Requirements:
- SSH & Python

Uses:
- Create Ansible User
- admin user -Core Manager
    - ssh key
    - sudoer/wheel status

- Manage Connection Perameters
    - ssh key
    - ssh port

- Server Security
    - fail2ban
    - firewall